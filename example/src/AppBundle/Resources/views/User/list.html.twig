{% extends "::layout.html.twig" %}

{% block body %}
  <div class="page-header">
    <h1>Users</h1>
  </div>

    {% for user in users %}
      <div class="well"><a href="{{ path("login", {id: user.id })}}">Login as {{ user }}</a></div>
    {% endfor %}
{% endblock %}
